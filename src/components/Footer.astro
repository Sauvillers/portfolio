---
export interface Props {
    paragraphs: string[];
}

const { 
    paragraphs,
} = Astro.props;
---
<footer class="px-12 py-6">
    { paragraphs.map(paragraph => <p data-animate set:html={paragraph}/> ) }
</footer>